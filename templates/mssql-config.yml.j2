integration_name: com.newrelic.mssql

instances:
  - name: mssql-server
    command: all_data
    arguments:
      hostname: mssql.localnet
      username: "{{ mssql_user }}"
      password: "{{ mssql_password }}"
      port: 1433
    labels:
      env: "{{env}}"
      role: mssql